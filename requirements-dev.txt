-r requirements.txt

# Building the Windows executable
pyinstaller>=6.6

# Tests
pytest>=8.0

# Optional, higher-accuracy morphological analysis for furigana.
# Not bundled into the executable by default because unidic-lite adds ~50 MB.
fugashi>=1.3
unidic-lite>=1.0.8
